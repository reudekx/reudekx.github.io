---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>{{ site.url }}/</loc>
    <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
    <changefreq>weekly</changefreq>
    <priority>1.0</priority>
  </url>
  {%- for page in site.pages -%}
    {%- unless page.url contains ".xml" or page.url contains "/assets" or page.sitemap.exclude == "yes" -%}
      <url>
        <loc>{{ site.url }}{{ page.url | remove: "index.html" }}</loc>
        <lastmod>{{ page.date | default: site.time | date_to_xmlschema }}</lastmod>
        <changefreq>{{ page.sitemap.changefreq | default: "monthly" }}</changefreq>
        <priority>{{ page.sitemap.priority | default: 0.5 }}</priority>
      </url>
    {%- endunless -%}
  {%- endfor -%}
  {%- for post in site.posts -%}
    {%- unless post.sitemap.exclude == "yes" -%}
      <url>
        <loc>{{ site.url }}{{ post.url }}</loc>
        <lastmod>{{ post.date | date_to_xmlschema }}</lastmod>
        <changefreq>{{ post.sitemap.changefreq | default: "monthly" }}</changefreq>
        <priority>{{ post.sitemap.priority | default: 0.7 }}</priority>
      </url>
    {%- endunless -%}
  {%- endfor -%}
  {%- for collection in site.collections -%}
    {%- unless collection.label == "posts" -%}
      {%- for doc in collection.docs -%}
        {%- unless doc.sitemap.exclude == "yes" -%}
          <url>
            <loc>{{ site.url }}{{ doc.url }}</loc>
            <lastmod>{{ doc.date | default: site.time | date_to_xmlschema }}</lastmod>
            <changefreq>{{ doc.sitemap.changefreq | default: "monthly" }}</changefreq>
            <priority>{{ doc.sitemap.priority | default: 0.6 }}</priority>
          </url>
        {%- endunless -%}
      {%- endfor -%}
    {%- endunless -%}
  {%- endfor -%}
</urlset>