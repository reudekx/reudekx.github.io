collections:
  articles:
    output: true
    permalink: /:path

defaults:
  - scope:
      path: ""
    values:
      layout: default
  - scope:
      path: "_articles"
    values:
      layout: article